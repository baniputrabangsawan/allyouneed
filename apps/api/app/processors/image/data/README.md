# Face detection models

`face_detection_yunet_2023mar.onnx` is OpenCV Zoo YuNet (2023-03). Used by Blur Face for detection and 5-point landmarks. Haar cascades remain a fallback when the ONNX file is missing.
